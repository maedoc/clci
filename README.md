# OpenCL on Travis CI (clci)

These are some template files to demo using AMD's OpenCL driver on Travis CI.

Script and notes for the `.travis` file taken from [OpenCL.jl](https://github.com/JuliaGPU/OpenCL.jl).
